# ⚠️  Python 3.12+ / 3.14: install pydantic with binary wheels to avoid Rust compilation:
#     pip install --only-binary=pydantic-core pydantic pydantic-settings
#     pip install -r requirements.txt
#
# Core framework — pydantic uses Rust; let pip resolve Python 3.14 compatible wheels
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.10.0          # pydantic-core 2.27+ has Python 3.14 wheels
pydantic-settings>=2.6.0

# Telegram Bot
python-telegram-bot==21.5

# AI / LLM
groq>=0.9.0
langgraph>=0.2.14
langchain-core>=0.2.38

# Stock Data
yfinance>=0.2.43
pandas>=2.2.0

# HTTP
httpx>=0.27.0

# Dev / Test
pytest>=8.3.0
pytest-asyncio>=0.23.8

# Utilities
python-dotenv>=1.0.0
